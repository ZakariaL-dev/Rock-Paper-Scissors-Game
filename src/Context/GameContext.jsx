import { createContext } from "react";

export const PlayerChoiceContext = createContext({});
export const ResultContext = createContext({});
export const ShowResultDisplayContext = createContext({});
export const ShowDashBoardContext = createContext({});
export const ScoresContext = createContext({});
